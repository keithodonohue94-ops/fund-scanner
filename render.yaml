services:
  - type: web
    name: fund-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --timeout 180
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true
